/**
 * Yes, this is pointless, i'm keeping it for the sake of keeping up the formula
 * @param input
 * @returns
 */
function prepareInput(input: string) {
  return input;
}

export default prepareInput;
