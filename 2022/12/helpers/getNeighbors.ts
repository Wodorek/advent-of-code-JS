function getNeighbors(row: number, col: number, terrain: string[][]) {
  // const alphabet = 'abcdefghijklmnopqrstuvwxyz'.split('');
  // const neighbors = [];
  // const currElevation = terrain[row][col];
  // const currElevationIdx = alphabet.indexOf(currElevation);
}

export default getNeighbors;
